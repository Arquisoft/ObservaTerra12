Esta base de datos escucha por defecto en el puerto 9001
El usuario es 'sa' y no tiene clave

--------------------------
En caso de que no se hayan descargado las librerias:
ejecutar
mvn dependency:copy-dependencies -DoutputDirectory=lib
Desde la carpeta principal del proyecto