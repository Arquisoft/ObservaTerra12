# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Index
GET     /                          			controllers.Application.index()
POST	/									controllers.Application.authenticate()

# Error
GET     /error                      		controllers.Application.error()

# Menu
GET     /user-panel                 		controllers.Application.userPanel()
GET		/logout								controllers.Application.logout()
GET		/user-panel/documents				controllers.Application.documents()

# Menu/Documentos
GET		/user-panle/documents/download-file	controllers.Application.downloadFile()

# Visualization

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               		controllers.Assets.at(path="/public", file)